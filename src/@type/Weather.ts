export type Item = {
    data:{
        condition?: string;
        max?: number;
        min?: number;
        weekday?: string;
    }
}
export type ItemWeather = {
    condition: string;
    max: number;
    min: number;
    weekday: string;
}

